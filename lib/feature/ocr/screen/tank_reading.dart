import 'package:flutter/material.dart';

class TankReading extends StatelessWidget {
  const TankReading({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text(' Tank Reading  ')));
  }
}
