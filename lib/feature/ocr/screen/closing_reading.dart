import 'package:flutter/material.dart';

class ClosingReading extends StatelessWidget {
  const ClosingReading({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('  Closing Successful ')));
  }
}
