import 'package:flutter/material.dart';

class OpeningReading extends StatelessWidget {
  const OpeningReading({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text(' Opening Reading  ')));
  }
}
